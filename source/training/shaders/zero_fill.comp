#version 450
layout(local_size_x = 128) in;
layout(std430, binding = 0) readonly buffer A { float dummy_a[]; };
layout(std430, binding = 1) readonly buffer B { float dummy_b[]; };
layout(std430, binding = 2) buffer C { float output_data[]; };
layout(push_constant) uniform P { int total; } pc;
void main() {
    uint i = gl_GlobalInvocationID.x;
    if (i >= uint(pc.total)) return;
    output_data[i] = 0.0;
}
